apiVersion: apps/v1
kind: Deployment
metadata:
  name: keeva-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: keeva-operator
  template:
    metadata:
      labels:
        name: keeva-operator
    spec:
      serviceAccountName: keeva-operator
      containers:
        - name: keeva-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - keeva-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "keeva-operator"
